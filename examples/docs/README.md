# Handbook

The sample tree `md-browser-editor` opens when you run it from the repository:

```sh
npx md-browser-editor serve examples/docs --open
```

Everything here is ordinary markdown — **bold**, *italic*, `inline code`, and
[links](https://example.com).

- a list item
- another one

> A blockquote, for the passages that quote someone.

---

Pick a page on the left, or press ＋ to make a new one.
